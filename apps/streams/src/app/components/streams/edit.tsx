import React from 'react';

/* eslint-disable-next-line */
export interface EditProps {}

export const StreamEdit = (props: EditProps) => {
  return (
    <div>
      <h1>Welcome to edit a stream page!</h1>
    </div>
  );
};

export default StreamEdit;
