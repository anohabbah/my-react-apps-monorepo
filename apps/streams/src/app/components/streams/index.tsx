import React from 'react';

/* eslint-disable-next-line */
export interface IndexProps {}

export const Index = (props: IndexProps) => {
  return (
    <div>Streams List Page</div>
  );
};

export default Index;
