import React from 'react';

/* eslint-disable-next-line */
export interface CreateProps {}

export const StreamCreate = (props: CreateProps) => {
  return (
    <div>
      <h1>Welcome to create stream page!</h1>
    </div>
  );
};

export default StreamCreate;
